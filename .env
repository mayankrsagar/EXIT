# ------------------------------------------------------------
# NODE & SERVER CONFIG
# ------------------------------------------------------------
# Set to “development” in local dev, and “production” on your deployed server
NODE_ENV=NODE_ENV

# The port your Express server will listen on
PORT=5000

# ------------------------------------------------------------
# MONGODB CONNECTION
# ------------------------------------------------------------
# Your MongoDB Atlas connection string (never commit with real credentials!)
MONGO_URI=mongodb+srv://mayankrsagar:exit@exit.cswni6d.mongodb.net/EXIT?retryWrites=true&w=majority&appName=EXIT


# ------------------------------------------------------------
# JWT (JSON WEB TOKEN)
# ------------------------------------------------------------
# Secret used to sign your JWTs — keep this truly secret!
JWT_SECRET=mayankIsHero

# How long sign‐in tokens remain valid (e.g. “7d” = 7 days)
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------
# CALENDARIFIC API (HOLIDAY CHECK)
# ------------------------------------------------------------
# Your Calendarific API key (sign up at https://calendarific.com if you haven’t)
CALENDARIFIC_API_KEY=Fnd3dCsYqf9CeYFipRu71qzoEA3BVoWL

# ------------------------------------------------------------
# EMAIL / SMTP CONFIGURATION (NODemailer)
# ------------------------------------------------------------
# To send email notifications (e.g., when an employee submits a resignation),
# you need to configure a valid SMTP server. Example below uses Mailtrap.io
# (https://mailtrap.io/) for development/testing, or you can substitute your own
# Gmail/SendGrid/AWS SES SMTP credentials in production.

# SMTP host (e.g., smtp.mailtrap.io OR smtp.gmail.com)
SMTP_HOST=sandbox.smtp.mailtrap.io

# SMTP port (587 is typical for TLS, 465 for SSL)
SMTP_PORT=587

# If you’re using port 465 (SSL), set this to true; otherwise “false”
SMTP_SECURE=false

# SMTP username (for Mailtrap, you’ll find this in your Mailtrap inbox settings)
SMTP_USER=43f30c6cd313b1

# SMTP password (for Mailtrap, you’ll find this in your Mailtrap inbox settings)
SMTP_PASS=2215c881da217f

# The “From” email address that Nodemailer will use, and the “To” address for HR.
# For example, you might send from no‐reply@mycompany.com to hr@mycompany.com.
HR_EMAIL=mayankrsagar@gmail.com

